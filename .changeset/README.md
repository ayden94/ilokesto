# Changesets

Add a changeset for consumer-facing package changes:

```bash
pnpm changeset
```

Packages are versioned independently. Use `patch` for fixes, `minor` for backward-compatible features, and `major` for breaking changes.
