---
title: "subscribe"
description: "리스너를 등록하고, 리스너 안에서 상태를 읽고, 구독을 정리합니다."
---

# subscribe

`subscribe()`는 리스너를 등록하고 cleanup 함수를 반환합니다.

```ts
subscribe(listener: () => void): () => void
```

리스너는 상태가 바뀐 뒤 호출됩니다. 인자를 받지 않으므로 최신 값이 필요하면 리스너 안에서 `getState()`를 호출하세요.

## 기본 사용법

```ts lineNumbers
const unsubscribe = store.subscribe(() => {
  console.log(store.getState());
});

store.setState((prev) => ({ ...prev, ready: true }));
unsubscribe();
```

## cleanup은 계약의 일부입니다

`subscribe()`를 호출할 때마다 내부 `Set`에 리스너가 추가됩니다. 반환된 구독 해제 함수를 호출하지 않으면 그 리스너는 계속 등록된 상태로 남습니다.

cleanup을 소유자의 lifecycle에 연결하세요.

```ts lineNumbers
function connectPanel(panel: { render(value: unknown): void; destroy(cb: () => void): void }) {
  const unsubscribe = store.subscribe(() => {
    panel.render(store.getState());
  });

  panel.destroy(unsubscribe);
}
```

## 알림 시점

리스너는 상태가 저장된 뒤 동기적으로 실행됩니다. store가 `Array.from(this.listeners)`를 순회하므로 알림 중에 추가된 리스너는 현재 알림 cycle에는 호출되지 않습니다. 알림 중에 제거된 리스너는 이미 그 cycle의 배열에 복사되어 있었다면 실행될 수 있습니다.

## 같은 값이면 알림이 없습니다

`setState()`가 `Object.is` 기준 같은 값 또는 같은 참조로 계산되면 리스너는 실행되지 않습니다.

```ts lineNumbers
const unsubscribe = store.subscribe(() => {
  console.log("changed");
});

store.setState((prev) => prev); // log 없음
unsubscribe();
```

## 파생된 값에 구독하기

`subscribe()`는 selector 오버로드도 받습니다. listener가 store 전체 대신 상태의 일부분(slice)에 반응할 수 있습니다.

```ts
subscribe<Selection>(
  selector: (state: Readonly<T>) => Selection,
  listener: (nextSelection: Selection, previousSelection: Selection) => void,
  equalityFn?: (previousSelection: Selection, nextSelection: Selection) => boolean
): () => void
```

listener는 구독을 등록하는 시점에 **즉시 호출되지 않습니다**. store가 업데이트되고 선택된 값이 바뀐 뒤에만 실행됩니다.

```ts lineNumbers
type User = { id: string; name: string };
type UserState = { user: User; revision: number };

const userStore = new Store<UserState>({
  user: { id: "1", name: "Ada" },
  revision: 0,
});

const unsubscribe = userStore.subscribe(
  (state) => state.user,
  (nextUser, previousUser) => {
    console.log("user changed:", previousUser.name, "->", nextUser.name);
  }
);

userStore.setState((prev) => ({
  ...prev,
  user: { ...prev.user, name: "Grace" },
  revision: prev.revision + 1,
}));

unsubscribe();
```

listener는 `(nextSelection, previousSelection)`를 인자로 받습니다. `nextSelection`으로 새 slice를 읽고, `previousSelection`으로 직전 값과 비교하세요.

기본 equality는 `Object.is`입니다. 매번 새 참조가 반환되지만 의미상으로는 같다고 봐야 하는 경우에는 직접 `equalityFn(previousSelection, nextSelection)`을 넘기세요.

```ts lineNumbers
const unsubscribe = userStore.subscribe(
  (state) => state.user,
  (nextUser) => {
    console.log("user identity changed:", nextUser.id);
  },
  (previousUser, nextUser) => previousUser.id === nextUser.id
);
```

equality 함수(기본 `Object.is` 또는 직접 넘긴 함수)가 이전/다음 selection을 같다고 판단하면 내부 상태 참조가 바뀌었더라도 그 업데이트에 대해서는 listener가 실행되지 않습니다. 이 동작이 selector 구독이 "실제로 관심 있는 slice에 영향이 없는 state 변경"으로 인해 재실행되는 것을 막아 줍니다.

selector 구독도 같은 내부 `Set`에 저장되므로, [알림 시점](#알림-시점), [같은 값이면 알림이 없습니다](#같은-값이면-알림이-없습니다), [cleanup은 계약의 일부입니다](#cleanup은-계약의-일부분입니다)에 정리된 규칙이 그대로 적용됩니다. 상태가 저장된 뒤 동기적으로 실행되고, `setState()`가 상태 레벨에서 같은 참조로 계산되면 실행되지 않으며, 반환된 unsubscribe 함수를 호출하면 selector listener가 제거됩니다.

## Listener error

store는 리스너 error를 잡지 않습니다. 어떤 리스너가 throw하면 `setState()` 호출도 throw하고, 그 알림 cycle의 뒤쪽 리스너가 실행되지 않을 수 있습니다. 리스너는 작게 유지하거나 예상 가능한 error는 리스너 안에서 잡으세요.
