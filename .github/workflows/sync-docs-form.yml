name: Sync Form Docs

on:
  push:
    branches: [main]
    paths:
      - packages/form/docs/**

jobs:
  sync:
    uses: ./.github/workflows/_sync-docs.yml
    with:
      package: form
    secrets: inherit
